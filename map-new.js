/* ============================================================================
   MAP.JS - MAP SECTION
   ============================================================================
   
   Controls the Mapbox map and all map-related functionality.
   
   CONTAINS:
   - Mapbox GL JS initialization
   - Map controls (geocoder, geolocate, compass)
   - Markers (single post icons)
   - Multi-post venue markers
   - Marker clusters (zoom < 8)
   - Small map cards (150x40 pills)
   - Big map cards (225x60 pills)
   - Hover map cards
   - Map shadow overlay
   - Zoom indicator
   - Spin animation
   
   CONTAINERS:
   - area (map display area)
   - controls (geocoder, geolocate, compass)
   - shadow (shadow overlay)
   - zoomindicator (zoom level display)
   - smallcard, bigcard, hovercard (map cards)
   - cluster (marker clusters)
   - multipost (multi-post venue markers)
   
   DEPENDENCIES:
   - index.js (backbone)
   - Mapbox GL JS (external API)
   
   COMMUNICATES WITH:
   - filter.js (map acts as filter)
   - post.js (clicking markers opens posts)
   - header.js (mode switch)
   - admin.js (map settings)
   
   ============================================================================ */

// Placeholder - to be built
console.log('[map.js] Map section loaded');

