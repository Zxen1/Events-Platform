/* ============================================================================
   HEADER.JS - HEADER SECTION
   ============================================================================
   
   Controls the header bar at the top of the page.
   
   CONTAINS:
   - Logo button (spin animation, welcome modal trigger)
   - Filter button (opens/closes filter panel)
   - Mode switch (Recents / Posts / Map buttons)
   - Access buttons (Member, Admin, Fullscreen)
   
   CONTAINERS:
   - bar (the header bar itself)
   - logo (logo button area)
   - filter (filter button)
   - modeswitch (3 mode buttons)
   - access (member/admin/fullscreen buttons)
   
   DEPENDENCIES:
   - index.js (backbone)
   
   COMMUNICATES WITH:
   - filter.js (opens filter panel)
   - member.js (opens member panel)
   - admin.js (opens admin panel)
   - post.js (mode switch affects display)
   - map.js (mode switch affects display)
   
   ============================================================================ */

// Placeholder - to be built
console.log('[header.js] Header section loaded');

